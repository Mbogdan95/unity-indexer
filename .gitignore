node_modules/
# dist/ intentionally not ignored — committed so the Claude Code plugin works without npm publish
*.db
*.db-wal
*.db-shm
.superpowers/
.env*
*.tsbuildinfo
coverage/
.DS_Store

docs/superpowers/

# local index cache (contains indexed project data — not for public repo)
.unity-indexer/

# local-only Claude settings
.claude/settings.local.json

# editor dirs
.idea/
.vscode/

*.log
